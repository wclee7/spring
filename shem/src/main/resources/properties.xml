<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd" >

<properties>
    <comment>설정</comment>
    <entry key="jdbc.driver">${jdbc.driver}</entry>
    <entry key="jdbc.url">${jdbc.url}</entry>
    <entry key="jdbc.username">${jdbc.username}</entry>
    <entry key="jdbc.password">${jdbc.password}</entry>
    <entry key="serverType">${serverType}</entry>
    <entry key="jdbc.initSize">${jdbc.initSize}</entry>
    <entry key="jdbc.maxActive">${jdbc.maxActive}</entry>
    <entry key="jdbc.maxIdle">${jdbc.maxIdle}</entry>
    <entry key="jdbc.minIdle">${jdbc.minIdle}</entry>

    <!-- bnd view -->
    <entry key="config.bndView">${config.bndView}</entry>
    <entry key="db.is-insert-necessary-datas">${db.init}</entry>
</properties>